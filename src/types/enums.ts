export enum OptionTypes {
    Correct = 'correct',
    Incorrect = 'incorrect',
    Selected = 'selected',
    Default = 'default'
}

export enum PrizeTypes {
    Next = 'next',
    Current = 'current',
    Previous = 'prev'
}

export enum SCREENS {
    START= 'start',
    GAME='game',
    END='end'
}
